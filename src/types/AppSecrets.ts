type AppSecrets = {
  MONGODB_URI: string;
  GEMINI_API_KEY: string;
  AWS_S3_BUCKET: string;
  AWS_REGION: string;
  GMAIL_USER: string;
  GMAIL_PASSWORD: string;
  JWT_SECRET: string;
};

export default AppSecrets;
